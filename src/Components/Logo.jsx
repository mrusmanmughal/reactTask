import logo from "../Assets/logo.png";
const Logo = () => {
  return (
    <div className="items-center justify-center flex">
      <img src={logo} alt="" width={80} className="rounded-full " />
    </div>
  );
};

export default Logo;
